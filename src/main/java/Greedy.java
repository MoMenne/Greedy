
public class Greedy {

    public static void main(String[] varArgs) {
//        if (varArgs.length > 1) {
        CalculateChange calculateChange = new CalculateChange(new USCoinCalculator());
        calculateChange.toString();
//            coinCalculator.usChangeBack(varArgs[0]);
//        }
//        throw new IllegalArgumentException("No value given");
    }
}
